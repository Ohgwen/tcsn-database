{
  "name": "tcsn-roster",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "db:migrate:local": "wrangler d1 migrations apply tcsn-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply tcsn-db --remote",
    "seed:sql": "node scripts/csv-to-sql.mjs",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
